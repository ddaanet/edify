# Recall Artifact: Update Prioritize Skill

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when cli commands are llm-native — `_prioritize` is internal/hidden, LLM-consumed
when designing cli tools for llm callers — JSON input / markdown output contract
when cli error messages are llm-consumed — error output for agent callers
when choosing script vs agent judgment — scoring arithmetic is non-cognitive, script it
when checking self-referential modification — prioritize skill modifying itself
when avoiding CLI skill name collision — naming the new CLI command
when routing prototype work through pipeline — prototype → CLI promotion path
when bootstrapping self-referential improvements — apply improvement before using improved tool
