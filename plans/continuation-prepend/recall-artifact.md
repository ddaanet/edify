# Recall Artifact: Continuation Prepend

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

how chain multiple skills together — continuation passing architecture, protocol design
when using hook based parsing — hook vs fragment parsing, deterministic continuation
when selecting model for prose artifact edits — opus for skills/fragments/agents
when adding a new variant to an enumerated system — grep downstream enumeration sites
when loading context before skill edits — plugin-dev skill-development description
