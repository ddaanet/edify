# Skill Review Recall Artifact: recall-null

Resolve ALL entries via `agent-core/bin/when-resolve.py` before reviewing.

## Entry Keys

when loading context before skill edits — description frontmatter dual duty (agent triggering + user display)
when placing DO NOT rules in skills — co-locate negative constraints with positive guidance
when skill sections cross-reference context — inline shared criteria at each usage site
when anchoring gates with tool calls — D+B pattern being propagated to skills
when selecting gate anchor tools — null mode rationale, preconditions match runtime state
how prevent skill steps from being skipped — convention: every skill step opens with tool call
