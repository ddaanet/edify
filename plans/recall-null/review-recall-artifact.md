# Review Recall Artifact: recall-null

Resolve ALL entries via `agent-core/bin/when-resolve.py` before reviewing.

## Entry Keys

when anchoring gates with tool calls — D+B pattern: tool call proves gate reached
when selecting gate anchor tools — null mode: equal-cost negative path
how prevent skill steps from being skipped — D+B hybrid convention, every step anchors with tool call
when selecting model for prose artifact edits — opus for skills/fragments/agents
when loading context before skill edits — description frontmatter dual duty
when holistic review applies fixes — grep all references to changed values
when adding a new variant to an enumerated system — check downstream enumeration sites
