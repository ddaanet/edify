# Recall Artifact: Precommit Python3 Redirect

Resolve entries via `plugin/bin/when-resolve.py` — do not use inline summaries.

## Entry Keys

when using hooks in subagents — hooks don't fire in Task sub-agents
when hook fragment alignment needed — hook output must reinforce not contradict fragments
when splitting validation into mechanical and semantic — script for deterministic, agent for judgment
when fixing behavioral deviations identified by RCA — structural fix (hooks/scripts) over prose
when using hook based parsing — deterministic hook parsing over fragment-based LLM inference
when adding a new variant to an enumerated system — grep all downstream enumeration sites
when choosing script vs agent judgment — deterministic pattern matching belongs in script
when placing quality gates — gate at chokepoint, scripted mechanical enforcement
when designing quality gates — layered defense, multiple independent checks
when execution routing preempts skill scanning — hook enforces what rules cannot
when sub-agent rules not injected — domain context must be carried explicitly
how configure script entry points — pyproject.toml scripts entry
when preferring e2e over mocked subprocess — real git repos in tmp_path
when green phase verification includes lint — just check && just test
