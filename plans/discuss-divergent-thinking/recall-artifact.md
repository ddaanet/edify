# Recall Artifact: Discuss Divergent Thinking

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when d: mode validates a proposed change — d: verdict chains to p: task creation
when directives outgrow text expansions — promotion from text expansion to skill with gates
when using conceptual explore — divergent work always delegates to opus
how wrap a discussion session — d: session wrap sequence
when writing methodology — ground before asserting methodology
when requirements capture needs recall — recall step in requirements skill
