# Recall Artifact: Skill-CLI Integration

Resolve entries via `edify _recall resolve` — do not use inline summaries.

## Entry Keys

when behavioral code — TDD gating for behavioral changes
when hook fragment alignment needed — hook output must reinforce fragment instructions
when skill steps — D+B anchor convention for skill edits
when writing hook redirect messages — include rationale in hook messages
when loading context before skill edits — load plugin-dev:skill-development before editing skills
when classifying settings.local.json entries during triage — allowed-tools vs permissions.allow independence
