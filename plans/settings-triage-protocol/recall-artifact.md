# Recall Artifact: Settings Triage Protocol

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when treating commits as sync points — commit skill as extension point for new versioned state
how prevent skill steps from being skipped — D+B hybrid pattern for triage step design
when placing quality gates — commit chokepoint, scripted mechanical enforcement
when fixing behavioral deviations identified by RCA — structural fix over prose strengthening
when editing skill files — description field, control flow, D+B gate additions
how verify commits defense in depth — layered commit verification
