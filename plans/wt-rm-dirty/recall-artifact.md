# Recall Artifact: wt-rm-dirty

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when removing worktrees with submodules — rm data-loss vectors, core.worktree
when tracking worktree tasks in session.md — _update_session_and_amend failure source
when validating worktree merges — remerge_session_md phase 4 context
when tests simulate merge workflows — test patterns for merge/amend scenarios
when requiring clean git tree — dirty-state handling policy
when cli error messages are llm-consumed — STOP directive patterns for rm output
