# Recall Artifact: Inline TDD Dispatch

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when delegating TDD cycles to test-driver — base decision being extended
when limiting agent scope — structural enforcement principle (context absence > prose gates)
when agent context has conflicting signals — persistent context wins, phase-neutral only in common
how write green phase descriptions — behavioral not prescriptive, relates to cycle isolation
when loading context before skill edits — plugin-dev skill-development required before editing /inline
