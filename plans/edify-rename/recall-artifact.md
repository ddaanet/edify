# Recall Artifact: Edify Rename

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when reusable components reference project paths — hardcoded paths in reusable code vs project config
when step file inventory misses codebase references — grep full tree for rename propagation
when git branch rename writes config — sandbox bypass for git config writes
when hook commands use relative paths — CLAUDE_PROJECT_DIR absolute resolution
when choosing name — discovery and recall over cleverness
