# Recall Artifact: Execute Skill Dispatch

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when choosing execution tier — tier model governs what "start task" means at each level
when using inline execution lifecycle — /inline wraps Tier 1/2 with recall + corrector
how dispatch corrector from inline skill — corrector template for post-execution review
when design resolves to simple execution — execution readiness gate, skip runbook
when companion tasks bundled into design invocation — companion work needs own triage pass
when adding a new variant to an enumerated system — grep downstream enumeration sites
when execution routing preempts skill scanning — structural fix: UPS hook skill-trigger injection
how chain multiple skills together — continuation passing, hook as parsing layer
when fixing behavioral deviations identified by RCA — structural fix, not prose strengthening
when anchoring gates with tool calls — D+B anchor proves gate was reached
when hook fragment alignment needed — hook recency must reinforce fragment primacy
how filter user prompt submit hooks — all filtering script-internal
when writing hook redirect messages — include rationale in hook messages
