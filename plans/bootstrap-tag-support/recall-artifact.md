# Recall Artifact: bootstrap-tag-support

## Entry Keys

when scoping corrector on TDD deliverables
when phase files contain h2 headers in code blocks
when concluding reviews
when batch changes span multiple artifact types
when routing implementation findings
