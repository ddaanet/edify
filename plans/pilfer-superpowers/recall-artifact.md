# Recall Artifact: pilfer-superpowers

Read each file listed below — do not rely on inline summaries.

## Entries

memory/strategic-pivot.md — superpowers was adopted as the baseline process skillset in the 2026-05 teardown; the pipeline revival partially reversed that, so the edify/superpowers relationship is the core context for any pilfering decision
memory/workflow-pipeline-revival.md — the revived pipeline is untested end-to-end and was rewired by reference-sweep; adoption work lands on top of this known-fragile base
memory/cc-subagent-context-capabilities.md — hard platform facts (no max_turns, no resume, child results never reach parent, Skill tool present in subagents) that constrain which superpowers delegation mechanics are adoptable as-is
memory/ddaanet/skill-description-purpose-first.md — user feedback rule for skill descriptions (purpose first, then triggers); conflicts with superpowers' triggers-only SDO policy and must govern the reconciliation
docs/design.md §6.4 "Pipeline contracts" — T1–T6.5 transformation/review-gate table and the fix-all reviewer policy; any imported review mechanic must be reconciled against this contract
docs/design.md §6.9 "Prompt and instruction structure" — existing grounded prompt-technique research (primacy bias, format-by-content-type, instruction density per model tier); overlaps superpowers' persuasion-principles, merge rather than duplicate
