# Recall Artifact: Skills Quality Pass

Resolve entries via `agent-core/bin/when-resolve.py` — do not use inline summaries.

## Entry Keys

how to prevent skill steps from being skipped — D+B hybrid pattern, core gate-fix approach
when placing do not rules in skills — negative constraint co-location
when selecting model for prose artifact edits — opus for all architectural artifacts
when refactoring agents need quality directives — explicit deslop directives in prompts
how to make skills discoverable — 4 discovery layers, description quality
when formatting rules for adherence — bullets vs prose by content type
when writing rules for different models — opus/sonnet/haiku instruction density
when designing quality gates — defense-in-depth layers, D+B as Layer 1
when bootstrapping self-referential improvements — tool-usage dependency ordering
when reviewing skill deliverable — cross-project context, skill-reviewer routing

