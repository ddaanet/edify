# TDD Recall Artifact: UserPromptSubmit Topic

Flat list for sub-agent resolve-all. Run: `claudeutils _recall resolve "<entry>" ...` for all entries below.

## Entry Keys

filter user prompt submit hooks
hook commands use relative paths
writing memory-index trigger phrases
