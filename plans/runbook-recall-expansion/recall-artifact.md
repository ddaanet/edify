# Recall Artifact: Runbook Recall Expansion

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when implementation modifies pipeline skills — self-modification coupling, inline task sequence
when writing recall artifacts — entry keys only, no excerpts
when editing skill files — extraction safety, trigger phrases, D+B gates
when writing multiline strings — dedent pattern for indented code
when reading validation command output — show full output, no truncation
