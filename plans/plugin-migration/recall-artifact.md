# Recall Artifact: Plugin Migration

Resolve entries via `edify _recall resolve` — do not use inline summaries.

## Entry Keys

when simple routing bypasses inline lifecycle — execution ceremony independent of classification
when checking complexity before expansion — callback to previous level if too large
when triaging behavioral code changes as simple — behavioral code routes to Moderate minimum
when reading design classification tables — read tables literally, apply judgment only where design says to
how validate migration conformance — tests bake expected behavior from reference into assertions
when hook commands use relative paths — all hooks must use $CLAUDE_PROJECT_DIR prefix
when using session start hooks — SessionStart output discarded for new interactive sessions (#10373)
when custom agents need session restart for discoverability — plan-specific agents need restart to index
when declaring phase type — tdd/general/inline determines expansion, review, orchestration
when phase qualifies as inline — no runtime feedback, prose/config, all decisions pre-resolved
