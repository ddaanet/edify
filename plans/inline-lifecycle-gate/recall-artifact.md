# Recall Artifact: inline-lifecycle-gate

## Resolved Entries

- "when anchoring gates with tool calls" — escape hatch IS the failure mode. Anchor with tool call that proves work happened.
- "when review gates feel redundant" — gates are non-negotiable checkpoints, not confidence-gated decisions.
- "when scoping corrector on TDD deliverables" — scope to ALL changed files, not most recent cycle.
- "when corrector rejects planning artifacts" — corrector reviews implementation only, planning → runbook-corrector.
- "how corrector dispatch from inline skill" — follows standardized template in corrector-template.md.
- "when triage feedback shows divergence" — divergence surfaced inline, no automatic action.
