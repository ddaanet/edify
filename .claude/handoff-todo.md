## Remaining

- Exercise the revived pipeline end to end on a small job, covering `Agent` dispatch, name-based resume, and the plan-specific agents that `prepare-runbook.py` generates.
- Fold the local corpus into a living design document.
- Settle the fate of `plugin/skills/recall/` and build it if it survives.