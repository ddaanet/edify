## Open decisions

- Whether two rejected memory facts should instead become upstream gitlore changes. Both were written and deleted as owned elsewhere: (a) composition recognizes only a plain link-first pointer bullet — the hook already prints "interleaved non-bullet line N inside the pointer block" with the remedy, but nothing signals the quiet half, that a non-pointer line placed *ahead* of the block composes cleanly, routes nothing, and makes `grep '^- \['` undercount the file the loader truncates; that sentence belongs in the `/gitlore:index-audit` skill. (b) `/gitlore:merge` step 2 says to re-run reconcile but never says that the same "memory merge prepared" block repeating for the same store means the merge is unlanded rather than a second divergence — the merger subagent can return a correct staged synthesis with `MERGE_HEAD` still set, and only `resolve.sh continue-after-merge` lands it. Decide whether to raise these against the gitlore repo.

## Remaining

- Write the design-doc-writing skill. Its three sources are the tier facts `ddaanet/design-doc-writing.md` (structure, the changelog split, splitting an oversized doc), `ddaanet/design-doc-rewire-dead-components.md`, `ddaanet/design-doc-no-situational-state.md`. Once it exists it becomes their owner under the memory-writing skill's §1.2 test — re-run that test over all three and retire into the skill whatever reduces to "when writing a design doc, do X".
- Dogfood the simplified pipeline end to end (`/design` → `/runbook` → `/orchestrate`) on a real plan; L-6 records it as unexercised, and FR-5's slice-batching claims stay unvalidated until it supplies counts. The refactor-dispatch naming, the two-commit-per-reviewed-slice rule, and the contract fixes from the `/code-review` pass were all settled on paper.
- Consolidate `memory/MEMORY.md` under the loader cutoff via `/gitlore:index-audit` — retire, don't shorten. Deliverable-review Major 10 is closed by L-5's retirement.
- `memory/ddaanet/design-doc-writing.md` is 18.6 KB against the 4 KB recall preview, so ~78% of it reaches a session only through a deliberate Read. Splitting it is the tier owner's call and belongs to the index-audit pass.
- Pilfer defect 21 (corrector-skeleton dedup), carrying: the unreachable "when reviewing runbooks/plans" criteria in `corrector.md` ~293–304; the corrector family's `Skill(edify:recall)` (`corrector.md:184`, `runbook-corrector.md:59`) against the by-path recall rule now stated once in `dispatch-composition.md` and `delegation.md`; `review-requirement.md`'s routing table still routing "plans" to `edify:corrector` with no outline/runbook rows (defers to D-26).
- FR-12 remainder beyond defect 21: per-class skill-body length targets, which have no calibration data.
- `/design plans/pilfer-superpowers/` once Q-1 and Q-3 settle.
- Report-only reviewers (`plugin-dev:skill-reviewer`, no Write) get truncated at ~4,000 chars in the reply; give `plugin/skills/inline/references/review-dispatch-template.md` a chunked-return instruction, or route skill definitions to a Write-capable reviewer.