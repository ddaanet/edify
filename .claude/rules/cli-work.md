---
paths:
  - "src/cli/**"
  - "bin/**"
---

When working on CLI-related files, read `docs/design.md` §6.1 "CLI and package" for established patterns and conventions.

Key areas: Error output patterns, output formats (text/JSON), entry point configuration
