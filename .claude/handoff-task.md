# Task — 2026-09-01

## Current task

Executing `plans/pipeline-simplification/outline.md` via `/inline plans/pipeline-simplification` (cold-start entry; BASELINE for triage-feedback = `a4aad0c8`). Tasks 0–7 are committed on `main`: `30906bc9` (tasks 0–1: baseline `reports/measurements.md` + deletions), `9c960340` (task 2: `runbook-outline-corrector` → `runbook-corrector`, simplifier reframed), `ab715e1a` (task 3: `/runbook` rewrite + `references/runbook-format.md`, five sources deleted), `f4bfa65a` (task 4: `/orchestrate` rewrite + `references/dispatch-composition.md`, `verify-step.sh` rewritten, `common-scenarios.md` folded in), `a7d9a1ce` (task 5: test-driver RED/GREEN modes, artisan/test-driver sonnet, corrector opus + slice-review protocols with the vacuous-green catalogue, refactor, tdd-auditor), `a8d0c3d2` (task 6: `/inline` trim, `/design` consumers, proof item-review, outline-corrector, fragments), `0bf8cc44` (task 7: `docs/design.md` D11 rewire, changelog entry, workflow-work rule, READMEs, CLAUDE.md, pilfer cross-refs). Deviation recorded: task 1 deleted five (not "seven") non-migrating runbook references, per the outline's Deletion timing paragraph.

**Task 8 in progress.** First `rg` sweep (removed names + tier vocabulary, excluding plans/, memory/, docs/changelog.md, docs/superpowers/, handoff files, tmp/) left: `plugin/agents/design-corrector.md:56` (`## Step`/`## Cycle` marker check → reword to `## Phase N:` / `Item N.M:`); `plugin/skills/review/SKILL.md:178` points at `references/common-scenarios.md` — check whether review/ has its own file of that name (it is not orchestrate's, which was deleted); `plugin/skills/review/references/example-execution.md:23` `plans/general-workflow/design.md` is an example path (harmless); `docs/design.md` and `CLAUDE.md` mentions of `prepare-runbook.py` / step files / `validate-runbook.py` sit in superseding and rejected-alternative prose — intentional write-time record, keep. Still to run: a scoped `rg` for `manifest` in the orchestrator sense only (`orchestrator manifest`, `step artifacts and an orchestrator manifest`), never bare `manifest`.

**Memory gate.** Task 7's rewrite of `memory/workflow-pipeline-revival.md` (present-tense: where pre-teardown history lives, stale→live name map, no commit ids — the memory hook rejects them) and its `MEMORY.md` index line are parked in `tmp/memory-parked/` (both files copied out; submodule restored with `git -C memory checkout`) because gitlore's pre-commit gate blocks every parent commit while memory is dirty and needs a user-approved summary. At the end: copy both back into `memory/`, present the summary blockquote, await approval, write `.claude/gitlore-memory-message`, commit. `memory/MEMORY.md` is 31.4KB against the 24.4KB loader cap — consolidation is out of this job's scope (`/gitlore:index-audit` later).