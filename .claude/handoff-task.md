## Current task

The deliverable-review fixes for `plans/pipeline-simplification` are delivered: four opus agents, one per artifact group, applied every Critical/Major/Minor finding except Major 10 (the L-5 size figure, held for the index-consolidation pass), with reports `fix-{orchestrate,agents,fragments,docs}.md` beside the review. The contracts now agree across orchestrate, agents, fragments and docs: slice commits are `<type>: Item N.M/k — <title>` with the type free and only the marker matchable; the orchestrator commits code-review fixes after (d), so a reviewed slice carries two marker commits and the auditor keys on the GREEN report's hash; report paths are mechanically `reports/<dispatch name>.md` (`item-N-M-s<k>-{red,test-review,green,code-review,refactor}`, `phase-P-corrector`, `final-review`, `tdd-audit`); `escalated:` re-dispatches refactor once on opus; the continuation fragment names no hook. `just precommit` green. Next is dogfooding the pipeline end to end — it is still unexercised.
