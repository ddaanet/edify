## Current task

`/inline plans/pipeline-simplification` has run every task of the outline (0–9): the deletion set is gone, `/runbook`, `/orchestrate`, the agents, fragments and the design record are rewired, after-measurements are in `plans/pipeline-simplification/reports/measurements.md`, and the four review-group reports under `plans/pipeline-simplification/reports/review-*.md` are applied with no UNFIXABLE. The rewritten `memory/workflow-pipeline-revival.md` (present-tense, stale-name → live-name map, no commit ids) and its `MEMORY.md` index line are back in the submodule awaiting the gitlore approval gate. The pipeline itself is still unexercised end to end; `/deliverable-review plans/pipeline-simplification` (opus, fresh session) is the named follow-up.
