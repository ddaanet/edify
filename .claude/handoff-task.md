## Current task

`/ddaa:preflight` ran against v0.1.1..HEAD — 18 commits, all plugin and docs, `src/edify/` untouched apart from two obsolete `validate_runbook` test files. Quality checks pass, both READMEs verify against reality, and the memory index resolves with no dangling links. Preflight only reports, so no release was run.

Two audit findings were acted on rather than filed. `CLAUDE.md` now imports `@memory/ddaanet/shared-claude.md`: that file declares itself the always-in-context tier for every repo mounting `ddaanet`, but edify had never wired the import, so its standing defaults reached no session here. And `docs/design.md` L-5 was retired rather than having its stale byte figure refreshed — a design record carries no situational state — with `§1 Now` and the `§7` rejected-alternative entry keeping the mechanism and naming `/gitlore:index-audit` as the pass that acts on it. L-6 keeps its id; the gap stands, since limitation ids are cited from the changelog, plans and memory.

Ahead of the design-doc-writing skill, the portable guidance that had been sitting in the project-local `feedback-decision-docs-are-living.md` moved into the `ddaanet` tier as one-fact files — `design-doc-no-situational-state` and `design-doc-rewire-dead-components`. The general rewire rule was already covered twice by the tier's `design-doc-writing.md`; only the dead-component-with-no-successor case was a genuine gap. The local file now holds edify's own instances and points at all three.