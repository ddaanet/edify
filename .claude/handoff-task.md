## Current task

`/deliverable-review plans/pipeline-simplification` is complete: `plans/pipeline-simplification/reports/deliverable-review.md` consolidates the two Layer 1 reports beside it (`deliverable-review-agentic-prose.md`, `deliverable-review-docs.md`) into 3 Critical / 13 Major / 24 Minor findings, every D10/D11 item and FR present, the gaps behavioural in the per-slice execution loop (uncommitted code-review fixes trip `verify-step.sh`; `tdd-auditor` keys on `feat:` subjects the gitmoji hook rewrites and that a fix/docs/perf slice would not carry anyway). Next is delivering those fixes by opus agent dispatch, grouped by artifact so each agent owns whole files: the `/orchestrate` loop and dispatch composition, the agents (`test-driver`, `tdd-auditor`, `refactor`, `runbook-simplifier`, `corrector`), the fragments and `/inline` routing, and the docs (`docs/design.md`, `plugin/README.md`, the outline's D5/D14). The pipeline itself is still unexercised end to end.
